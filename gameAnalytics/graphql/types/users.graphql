type User {
  id: Int!
  username: String!
  password: String!
  email: String!
}

type Query {
  getUsers: [User]!
}

type Mutation {
  createUser(username: String!, password: String!, email: String!): User!
  deleteUser(id: Int!): User!
}
